ВОПРОСЫ:
2) как делать запросы по нескольким типам данным - при выводе публикаций разных но не всех типов (а-ля content_type = type1 OR type2)
3) как обработать создание невалидных комментариев: (к статьям которых нет, если вбить в строку браузера ID и тип)
4) в комментариях почему сбрасывается commenting_object (в form_valid)
5) русские символы в комментариях (в дб прописано utf-8, в base.html - charset UTF-8)
6) можно ли наследоваться от просто формы (forms.Form) для создания CreateView, или же необходимо всегда наследоваться от ModelForm. Как в таком случае прописать параметры для полей (например, label, максимальный размер и тп.)?
